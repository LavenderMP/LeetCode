# Given two non-empty linked lists
# contains non-negative int numbers.
# sorted in reverse order.

l1 = [2,4,3]
l2 = [5,6,4]
class ListNode:
    def __init__(self, x):
        self.val = x
        self.next = None
# does l1, l2 are the same length?
def addLinkedList(arr):
    
print(ListNode)